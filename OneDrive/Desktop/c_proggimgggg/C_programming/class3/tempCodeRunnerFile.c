#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("enter the first number");
    scanf("%d",&a);
    printf("enter the first number");
    scanf("%d",&b);
    sum=a+b;
    printf("the sum of two number is %d",sum);
    return 0;
}